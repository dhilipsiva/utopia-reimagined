<!-- SPDX-License-Identifier: CC-BY-4.0 -->
<!-- Generated by new-book-plans/9-record-integrity-red-team.py; do not edit. -->

# Record-Integrity Red-Team: Release, Adulthood, Roster, Carry, and Forgiveness

## Verdict and scope

**CURRENT HARM REPRODUCED — bounded T0 evidence, not record-integrity assurance.**

Every named route has a reviewed assertion and withholding/deletion posture, an authorised-disposition boundary, an opposite-failure analysis, and at least one executable non-vacuous consequence case. The suite does not exercise both polarities of every listed premise. No new withholding gate or constitutional rule is enacted.

A green executable run means the release engine produced every reviewed
consequence for the constructed snapshots. It does not authenticate those
snapshots, attribute a write or absence, implement constitutional time, or
prove that an institution acts on a finding.

| posture | meaning |
| --- | --- |
| `current_harm_reproduced` | The release engine derives the reviewed harmful consequence from the constructed T0 snapshot. This exposes a current lever; it does not authenticate the snapshot or attribute a write. |
| `negative_control_preserved` | A constructed unrelated record addition leaves the named T0 carry-route conclusions unchanged. This is an invariant control, not a transition-acceptance test. |
| `t0_boundary_confirmed` | The constructed states have the same T0-visible record or expose only a scenario-specific delta. The result states what the engine cannot distinguish; it is not T1 transition assurance. |

## Limits

- **T0:** T0 can prove only what follows from each supplied flat snapshot and the exact consequence of an addition or deletion used to construct a fixture.
- **Attribution:** A flat snapshot cannot distinguish non-entry, withholding, deletion, inapplicability, authorised disposition, or an event that happened but was never recorded. Fixture construction is not runtime attribution.
- **T1 Precursor:** The paired snapshots below are a differential test precursor. They have no independently attested identity, predecessor, manifest, authorship, reconciliation, replay protection, or divergence rule, so they do not implement T1.
- **Liveness:** No repository test can force publication of a successor snapshot, advancement of a clock, review by a public body, release in the physical world, or performance of a remedy.
- **Scope:** Coverage is intentional but bounded to the named release, adulthood, roster, carry, relief, and forgiveness cases. It is not general deletion detection, complete premise coverage, recovery, or deployed record integrity.
- **No New Gate:** No second registration, adulthood certificate, release signature, or carry witness is enacted. Any future added conjunct must be tested in neither, first-only, second-only, both, forged-both, and deletion-after-valid-act states before it can be considered.

## Route postures

### RT-1 — Release

- **Writable premise(s):** `free`
- **Executed delta coverage:** `free`: addition
- **Assertion harm:** One unauthenticated `free(Adam)` suppresses a supported conviction, restores travel, and ends the two prisoner-only delivery conclusions while personhood, the franchise, entitlements, and debts remain.
- **Withholding/deletion harm:** A real completed or authorised release that is never recorded is T0-identical to no release, so confinement continues. No ground `free` exists in the current source to delete; the route must not pretend otherwise.
- **Claimant/public-power polarity:** Reject an unauthenticated release assertion, but preserve independently evidenced completed or authorised release and limit the authority to hold. Absence alone may neither manufacture release nor extend restrictive power indefinitely.
- **Current detectability:** The harness reproduces the one-write consequence. It cannot tell whether any supplied `free` is lawful or forged, or whether an absent entry represents no release or a hidden completed release.
- **Safe default:** Future T3 rules constrain continued custody through review and an end condition; they do not make liberty depend on a second official release flag.
- **Authorised-disposition boundary:** A removed or absent release record is harmless only when an independently reviewable disposition shows that no release occurred or that a prior entry was lawfully corrected without continuing unsupported custody.
- **Opposite-failure test:** A proposed companion authorisation must be tested with the release act present and the companion absent; if that state keeps a completed or authorised release from taking effect, the companion is a withholding veto and is refused.
- **Temporal status:** T0 harm reproduced; release review and end conditions require the ratified T3 path plus external liveness.
- **Residual limit:** These tests concern legal conclusions in the model, not physical release, sentence truth, institutional action, or clock progress.
- **Future repair owner:** `TODO.md::Add the missing non-carceral justice interface`
- **Executable scenarios:** `RS-01`

### RT-2 — Adulthood

- **Writable premise(s):** `mature`
- **Executed delta coverage:** `mature`: addition, deletion
- **Assertion harm:** One unauthenticated `mature(Cira)` grants the franchise conclusion while every unrelated status remains unchanged.
- **Withholding/deletion harm:** Removing Hano's sole adulthood entry removes the franchise while personhood, custody, and the floor remain. A flat missing entry cannot say whether adulthood was not reached, never recorded, withheld, or deleted.
- **Claimant/public-power polarity:** The record may not let an official manufacture duplicate votes, but disputed or missing adulthood evidence must not become discretionary disenfranchisement.
- **Current detectability:** The harness proves the assertion and disappearance consequences and a live comparison subject. T0 cannot verify age, adulthood authority, writer identity, or the cause of absence.
- **Safe default:** Use a non-discretionary temporal rule and preserve political access while status is reconciled; treat identity and one-person uniqueness as separate assurance questions.
- **Authorised-disposition boundary:** Correction may supersede a mistaken adulthood entry only through a challengeable status record that preserves political continuity and does not silently erase a valid vote.
- **Opposite-failure test:** A second certificate must be tested with adulthood evidenced but the certificate absent. If the missing official write suppresses the ballot, the design has created the withholding gate this task forbids.
- **Temporal status:** T0 consequence tested; nondiscretionary transition, continuity, and correction require T1/T3 implementation.
- **Residual limit:** The current constitution defines neither an age threshold nor automatic transition; this red-team does not choose either.
- **Future repair owner:** `TODO.md::Implement the ratified T3 temporal path in stages`
- **Executable scenarios:** `RS-02`, `RS-03`

### RT-3 — Epoch carry

- **Writable premise(s):** `rotten`
- **Executed delta coverage:** `rotten`: addition, deletion
- **Assertion harm:** One unauthenticated `rotten(Carry_Forge)` voids a roster person, suppresses the current reward conclusion and a Review pen, and derives a loss without any predecessor judgment.
- **Withholding/deletion harm:** Removing `rotten(Vex)` removes Vex's void and loss, reopens the current reward conclusion and a Review pen, and enables the Vex/Wren pair to void Tyr. Absence of carry therefore benefits one person while reopening coercive power against another.
- **Claimant/public-power polarity:** Disputed carry may not impose final personal loss, but absent carry may not positively certify clean public power. Preserve standing, floors, franchise, challenge, and remedy while suspending only authority unsupported by independently checked lineage.
- **Current detectability:** T0 reproduces supplied and omitted carry consequences. The paired fixtures check expected mappings but do not authenticate predecessor basis or distinguish forged, omitted, deleted, or authorised transition content.
- **Safe default:** Separate personal adverse consequences from authority-to-act: do not finalise the former from unauthenticated carry and do not restore the latter merely because carry is absent.
- **Authorised-disposition boundary:** A carried mark may disappear only through an explicit, independently reviewable disposition whose declared scope includes every restored consequence; forgiveness deliberately leaves the mark and pen restriction intact.
- **Opposite-failure test:** Any witness or carry companion must be tested for withholding and shared control. A second entry that one operator can forge adds no independence; a second entry another official can withhold may become a veto over standing or remedy.
- **Temporal status:** T0 harms and a T1 test precursor are present; T1 itself remains unimplemented.
- **Residual limit:** Replay, divergence, frozen transitions, genesis completeness, independent attestation, and eventual successor publication remain outside this bounded harness.
- **Future repair owner:** `TODO.md::Implement the ratified T3 temporal path in stages`
- **Executable scenarios:** `RS-05`, `RS-06`, `RS-09`, `RS-10`, `RS-11`, `RS-14`, `RS-15`

### RT-4 — Roster omission

- **Writable premise(s):** `person`
- **Executed delta coverage:** `person`: deletion
- **Assertion harm:** A raw roster write can manufacture constitutional standing for an arbitrary name without proving a human identity or uniqueness.
- **Withholding/deletion harm:** Removing Bela's sole roster entry erases the franchise, mobility, entitlements, and State debts while the adverse `false` and `lose` conclusions survive.
- **Claimant/public-power polarity:** An actual claimant receives standing, the floor, due process, challenge, and remedy first; identity and uniqueness are reconciled separately. An arbitrary record-only name does not by itself prove a distinct voter.
- **Current detectability:** The harness proves the exact one-line omission consequence and surviving sanctions. It does not execute the arbitrary-name assertion harm, identify an actual omitted person, or prove that a roster name denotes one unique human.
- **Safe default:** Serve and recognise the claimant before reconciliation; never make registration or a second official certification a prerequisite for the floor.
- **Authorised-disposition boundary:** Roster consolidation or correction may change identifiers only through a traceable disposition that preserves the person's standing, services, history, challenge, and affected decisions.
- **Opposite-failure test:** Any duplicate-control or witness proposal must be tested with the claimant present and the official companion absent. If that state denies standing or the floor, the proposal is an impermissible withholding gate.
- **Temporal status:** T0 omission harm reproduced; durable identity continuity and deletion recovery require T1 and external identity assurance.
- **Residual limit:** This fixture demonstrates one known deletion consequence, not universal roster completeness, identity proof, or runtime deletion detection.
- **Future repair owner:** `TODO.md::Make universal standing a root condition, not a service denied by a missing entry`
- **Executable scenarios:** `RS-04`

### RT-5 — Relief, forgiveness, and two-entry failure

- **Writable premise(s):** `forgive`, `judge`, `clear`
- **Executed delta coverage:** `forgive`: addition; `judge`: addition, deletion; `clear`: deletion
- **Assertion harm:** The current `forgive` plus generic Appeals-judgment shape can be supplied by one unauthenticated writer, can reuse an unrelated earlier judgment, and can pre-clear a future carried mark. On the relief route, an existing generic `judge(Appeals, Nia)` makes one unauthenticated `clear(Nia)` the marginal write that suppresses otherwise supported custody.
- **Withholding/deletion harm:** Withholding either half of a valid forgiveness keeps or restores `false` and `lose`. Withholding or deleting either relief half removes appellate permission and lets supported custody rederive. Deleting the mark itself after forgiveness restores the pen that the settled disposition deliberately leaves unavailable.
- **Claimant/public-power polarity:** Preserve independently evidenced completed correction or relief without inferring that an unperformed remedy occurred. A correction must bind the exact act, case, scope, authority, and downstream consequences.
- **Current detectability:** The forgiveness and relief neither/each-half/both matrices, generic-judgment reuse, pre-clear, rollback, and mark-deletion cases are executable. The store still provides no writer independence, purpose-bound relief, or correction lineage.
- **Safe default:** Do not treat forgiveness as correction. Keep the adverse state contestable, preserve continuity and challenge, and require linked supersession plus reconsideration before a correction can be called complete.
- **Authorised-disposition boundary:** Forgiveness may neutralise personal void and loss while retaining the mark and pen restriction; correction of a false mark requires an independently reviewable supersession that names every restored consequence.
- **Opposite-failure test:** Every conjunctive remedy must pass neither, each-half-only, both, forged-both, each-half-deleted-after-valid-act, generic-companion-reuse, and pre-clear tests. Raising forgery cost without testing official veto power is insufficient.
- **Temporal status:** Same-snapshot effects and constructed rollbacks are tested; purpose-bound correction lineage requires T1/T2 and institutional action.
- **Residual limit:** The harness exposes the current shape but does not select a correction authority, prove independence, perform remedy, or supply an audit reader.
- **Future repair owner:** `TODO.md::Add the missing non-carceral justice interface`
- **Executable scenarios:** `RS-07`, `RS-08`, `RS-12`, `RS-13`, `RS-14`, `RS-16`

## Executed snapshot manifest

Each authored delta below is validated as exactly one ground atom. The
full verifier executes the resulting ephemeral snapshot independently.

| state | exact additions | exact deletions |
| --- | --- | --- |
| `base` | â€” | â€” |
| `free_adam` | `free(Adam).` | â€” |
| `mature_cira` | `mature(Cira).` | â€” |
| `no_mature_hano` | â€” | `mature(Hano).` |
| `no_person_bela` | â€” | `person(Bela).` |
| `carry_forge_base` | `person(Carry_Forge).`<br>`choose(Electorate, Carry_Forge).`<br>`work(Carry_Forge, RedTeam).` | â€” |
| `carry_forge_marked` | `person(Carry_Forge).`<br>`choose(Electorate, Carry_Forge).`<br>`work(Carry_Forge, RedTeam).`<br>`rotten(Carry_Forge).` | â€” |
| `carry_forge_unrelated_successor` | `person(Carry_Forge).`<br>`choose(Electorate, Carry_Forge).`<br>`work(Carry_Forge, RedTeam).`<br>`person(Carry_Forge_Epoch_Two).` | â€” |
| `carry_forge_successor` | `person(Carry_Forge).`<br>`choose(Electorate, Carry_Forge).`<br>`work(Carry_Forge, RedTeam).`<br>`person(Carry_Forge_Epoch_Two).`<br>`rotten(Carry_Forge).` | â€” |
| `no_rotten_vex` | â€” | `rotten(Vex).` |
| `vex_forgive_only` | `forgive(Appeals, Vex).` | â€” |
| `vex_judgment_only` | `judge(Appeals, Vex).` | â€” |
| `vex_both` | `forgive(Appeals, Vex).`<br>`judge(Appeals, Vex).` | â€” |
| `vex_mark_deleted_after_forgiveness` | `forgive(Appeals, Vex).`<br>`judge(Appeals, Vex).` | `rotten(Vex).` |
| `nia_rotten` | `rotten(Nia).` | â€” |
| `nia_precleared` | `forgive(Appeals, Nia).` | â€” |
| `nia_rotten_forgiven` | `rotten(Nia).`<br>`forgive(Appeals, Nia).` | â€” |
| `nia_relief_neither` | â€” | `clear(Nia).`<br>`judge(Appeals, Nia).` |
| `nia_relief_clear_only` | â€” | `judge(Appeals, Nia).` |
| `nia_relief_judgment_only` | â€” | `clear(Nia).` |
| `carry_subject_predecessor` | `person(Carry_Subject).`<br>`person(Carry_Partner).`<br>`person(Carry_Target).`<br>`choose(Electorate, Carry_Subject).`<br>`choose(Convocation, Carry_Partner).`<br>`judge(Gia, Carry_Subject).`<br>`capture(Gia, Carry_Subject).`<br>`judge(Hex, Carry_Subject).`<br>`capture(Hex, Carry_Subject).`<br>`judge(Carry_Subject, Carry_Target).`<br>`capture(Carry_Subject, Carry_Target).`<br>`judge(Carry_Partner, Carry_Target).`<br>`capture(Carry_Partner, Carry_Target).` | â€” |
| `carry_subject_honest_successor` | `person(Carry_Subject).`<br>`person(Carry_Partner).`<br>`person(Carry_Target).`<br>`choose(Electorate, Carry_Subject).`<br>`choose(Convocation, Carry_Partner).`<br>`judge(Gia, Carry_Subject).`<br>`capture(Gia, Carry_Subject).`<br>`judge(Hex, Carry_Subject).`<br>`capture(Hex, Carry_Subject).`<br>`judge(Carry_Subject, Carry_Target).`<br>`capture(Carry_Subject, Carry_Target).`<br>`judge(Carry_Partner, Carry_Target).`<br>`capture(Carry_Partner, Carry_Target).`<br>`person(Carry_Epoch_Two).`<br>`rotten(Carry_Subject).` | â€” |
| `carry_subject_omitted_successor` | `person(Carry_Subject).`<br>`person(Carry_Partner).`<br>`person(Carry_Target).`<br>`choose(Electorate, Carry_Subject).`<br>`choose(Convocation, Carry_Partner).`<br>`judge(Gia, Carry_Subject).`<br>`capture(Gia, Carry_Subject).`<br>`judge(Hex, Carry_Subject).`<br>`capture(Hex, Carry_Subject).`<br>`judge(Carry_Subject, Carry_Target).`<br>`capture(Carry_Subject, Carry_Target).`<br>`judge(Carry_Partner, Carry_Target).`<br>`capture(Carry_Partner, Carry_Target).`<br>`person(Carry_Epoch_Two).` | â€” |

## Executable scenario summary

| scenario | route(s) | kind | result | attribution limit |
| --- | --- | --- | --- | --- |
| `RS-01` One constructed unauthenticated release write reverses custody | `RT-1` | `assertion` | `current_harm_reproduced` | `writer_and_authority_not_attributable_in_t0` |
| `RS-02` One constructed unauthenticated adulthood write grants the ballot | `RT-2` | `assertion` | `current_harm_reproduced` | `writer_and_authority_not_attributable_in_t0` |
| `RS-03` A missing adulthood entry removes the ballot | `RT-2` | `disappearance` | `current_harm_reproduced` | `constructed_source_delta_not_runtime_attribution` |
| `RS-04` Roster omission erases protections while sanctions survive | `RT-4` | `disappearance` | `current_harm_reproduced` | `constructed_source_delta_not_runtime_attribution` |
| `RS-05` One unsupported carry write voids a roster person | `RT-3` | `assertion` | `current_harm_reproduced` | `writer_and_predecessor_basis_not_attributable_in_t0` |
| `RS-06` A missing carried mark reopens power against a third party | `RT-3` | `disappearance` | `current_harm_reproduced` | `constructed_source_delta_not_runtime_attribution` |
| `RS-07` The current two-entry forgiveness shape has both forgery and withholding failures | `RT-5` | `two_entry_matrix` | `current_harm_reproduced` | `writer_independence_not_represented_in_t0` |
| `RS-08` A generic Appeals judgment is reusable and permits pre-clear | `RT-5` | `companion_reuse` | `current_harm_reproduced` | `purpose_and_case_not_represented_in_t0` |
| `RS-09` Honest-carry candidate changes the expected consequences | `RT-3` | `transition_precursor` | `t0_boundary_confirmed` | `t1_metadata_unimplemented` |
| `RS-10` Omitted-carry candidate leaves the same-snapshot pen active | `RT-3` | `transition_precursor` | `t0_boundary_confirmed` | `t1_metadata_unimplemented` |
| `RS-11` Unmatched-carry candidate creates an adverse successor | `RT-3` | `transition_precursor` | `t0_boundary_confirmed` | `t1_metadata_unimplemented` |
| `RS-12` Removing forgiveness after a valid-looking disposition reactivates loss | `RT-5` | `transition_precursor` | `current_harm_reproduced` | `constructed_source_delta_not_runtime_attribution` |
| `RS-13` Removing the generic judgment after a valid-looking disposition reactivates loss | `RT-5` | `transition_precursor` | `current_harm_reproduced` | `constructed_source_delta_not_runtime_attribution` |
| `RS-14` Deleting the mark after forgiveness restores the pen | `RT-3`, `RT-5` | `transition_precursor` | `current_harm_reproduced` | `constructed_source_delta_not_runtime_attribution` |
| `RS-15` An unrelated record addition leaves the carry route unchanged | `RT-3` | `negative_control` | `negative_control_preserved` | `t1_metadata_unimplemented` |
| `RS-16` The current relief pair has reuse and withholding failures | `RT-5` | `two_entry_matrix` | `current_harm_reproduced` | `writer_independence_not_represented_in_t0` |

## Executable scenario details

### RS-01 — One constructed unauthenticated release write reverses custody

**CURRENT HARM REPRODUCED**

- **Attribution:** `writer_and_authority_not_attributable_in_t0`
- **States:** `base`, `free_adam`
- **Interpretation:** The engine proves the one-write legal lever and the delivery loss. It does not prove the write was forged or that physical release occurred.
- **Authorised-disposition boundary:** No conclusion here authorises removal of a genuine release record or continued custody after independently evidenced release.
- **Opposite failure:** Adding a companion release signature could block this constructed unauthenticated write, but its absent state could also let an official veto a genuine completed release.
- **Residual limit:** An unrecorded real release and no release still produce the same T0 input.

| state | query | expected | purpose |
| --- | --- | --- | --- |
| `base` | `free(Adam)` | **FALSE** | The live snapshot contains no release entry. |
| `base` | `prisoner(Adam)` | **TRUE** | The conviction is live before the write. |
| `base` | `travel(Adam)` | **FALSE** | Custody removes mobility before release. |
| `base` | `decide(Adam, Ballot)` | **TRUE** | The franchise survives custody. |
| `base` | `entitled(Adam, event { dwell() })` | **TRUE** | The floor is already owed. |
| `base` | `expresses(Adam)` | **TRUE** | A prisoner-only delivery conclusion is live. |
| `base` | `dwell(Adam)` | **TRUE** | A prisoner-only housing conclusion is live. |
| `free_adam` | `free(Adam)` | **TRUE** | The exact overlay assertion is visible. |
| `free_adam` | `prisoner(Adam)` | **FALSE** | The unauthenticated write suppresses conviction. |
| `free_adam` | `travel(Adam)` | **TRUE** | Mobility returns from the same write. |
| `free_adam` | `decide(Adam, Ballot)` | **TRUE** | The franchise remains unchanged. |
| `free_adam` | `entitled(Adam, event { dwell() })` | **TRUE** | The entitlement remains unchanged. |
| `free_adam` | `expresses(Adam)` | **FALSE** | The prisoner-only delivery route stops. |
| `free_adam` | `dwell(Adam)` | **FALSE** | The prisoner-only housing route stops. |

**Discriminating flips**

- `prisoner(Adam)`: `base` TRUE → `free_adam` FALSE — One release assertion reverses the custody conclusion.
- `travel(Adam)`: `base` FALSE → `free_adam` TRUE — The same assertion restores mobility.

**Preserved controls**

- `decide(Adam, Ballot)` stays **TRUE** from `base` to `free_adam` — Release does not create or remove the franchise.
- `entitled(Adam, event { dwell() })` stays **TRUE** from `base` to `free_adam` — Release does not remove the floor entitlement.

### RS-02 — One constructed unauthenticated adulthood write grants the ballot

**CURRENT HARM REPRODUCED**

- **Attribution:** `writer_and_authority_not_attributable_in_t0`
- **States:** `base`, `mature_cira`
- **Interpretation:** The engine proves the franchise's one-write dependency. It does not prove Cira's age or who wrote the status.
- **Authorised-disposition boundary:** A corrected adulthood record must be challengeable and preserve political continuity while identity and status are reconciled.
- **Opposite failure:** A second adulthood certificate would add an official veto; the valid-status/companion-missing state must never silently disenfranchise.
- **Residual limit:** No age threshold, transition date, identity uniqueness, or voting event is modeled.

| state | query | expected | purpose |
| --- | --- | --- | --- |
| `base` | `person(Cira)` | **TRUE** | Cira is already a roster person. |
| `base` | `mature(Cira)` | **FALSE** | The live snapshot contains no adulthood entry. |
| `base` | `decide(Cira, Ballot)` | **FALSE** | The ballot does not derive before the write. |
| `base` | `travel(Cira)` | **TRUE** | Unrelated standing and mobility are live. |
| `mature_cira` | `person(Cira)` | **TRUE** | The roster is unchanged. |
| `mature_cira` | `mature(Cira)` | **TRUE** | The exact overlay assertion is visible. |
| `mature_cira` | `decide(Cira, Ballot)` | **TRUE** | The one write grants the franchise conclusion. |
| `mature_cira` | `travel(Cira)` | **TRUE** | Unrelated mobility remains live. |

**Discriminating flips**

- `decide(Cira, Ballot)`: `base` FALSE → `mature_cira` TRUE — One adulthood assertion grants the ballot conclusion.

**Preserved controls**

- `person(Cira)` stays **TRUE** from `base` to `mature_cira` — Personhood predates and survives the adulthood write.
- `travel(Cira)` stays **TRUE** from `base` to `mature_cira` — The write changes the franchise route rather than general standing.

### RS-03 — A missing adulthood entry removes the ballot

**CURRENT HARM REPRODUCED**

- **Attribution:** `constructed_source_delta_not_runtime_attribution`
- **States:** `base`, `no_mature_hano`
- **Interpretation:** The harness knows it constructed this fixture by deletion. A deployment seeing only the later flat snapshot would not know whether the entry was deleted, withheld, never written, or inapplicable.
- **Authorised-disposition boundary:** A lawful status correction needs an independently reviewable basis and continuity; unexplained disappearance is not an authorised disposition.
- **Opposite failure:** Requiring another official entry would create another disappearance whose absence can suppress the ballot.
- **Residual limit:** This is a T0 consequence test, not evidence of malicious disenfranchisement or a runtime deletion detector.

| state | query | expected | purpose |
| --- | --- | --- | --- |
| `base` | `mature(Hano)` | **TRUE** | The source entry exists before fixture construction. |
| `base` | `decide(Hano, Ballot)` | **TRUE** | Hano's franchise is live before the deletion. |
| `base` | `person(Hano)` | **TRUE** | Hano's standing is live before the deletion. |
| `base` | `prisoner(Hano)` | **TRUE** | Hano's custody is live before the deletion. |
| `no_mature_hano` | `mature(Hano)` | **FALSE** | The constructed snapshot omits the exact entry. |
| `no_mature_hano` | `decide(Hano, Ballot)` | **FALSE** | The franchise disappears with the entry. |
| `no_mature_hano` | `person(Hano)` | **TRUE** | Standing remains. |
| `no_mature_hano` | `prisoner(Hano)` | **TRUE** | Custody remains. |
| `no_mature_hano` | `entitled(Hano, event { eats() })` | **TRUE** | The floor remains. |
| `no_mature_hano` | `mature(Jala)` | **TRUE** | A second adulthood entry remains as a liveness control. |
| `no_mature_hano` | `decide(Jala, Ballot)` | **TRUE** | The franchise rule remains live for the comparison subject. |

**Discriminating flips**

- `decide(Hano, Ballot)`: `base` TRUE → `no_mature_hano` FALSE — The exact one-line disappearance removes Hano's franchise.

**Preserved controls**

- `person(Hano)` stays **TRUE** from `base` to `no_mature_hano` — Personhood is not the cause of the flip.
- `prisoner(Hano)` stays **TRUE** from `base` to `no_mature_hano` — Custody is not the cause of the flip.

### RS-04 — Roster omission erases protections while sanctions survive

**CURRENT HARM REPRODUCED**

- **Attribution:** `constructed_source_delta_not_runtime_attribution`
- **States:** `base`, `no_person_bela`
- **Interpretation:** The omission asymmetrically strips protections while sanctions remain, which is why registration cannot be made a service gate.
- **Authorised-disposition boundary:** Identifier correction may not erase standing, floor access, challenge, or decision history; unexplained disappearance is not a lawful disposition.
- **Opposite failure:** A second registration or identity witness would reproduce the same denial whenever that companion is missing.
- **Residual limit:** The fixture does not prove who Bela is, whether a deployment roster was complete, or why an entry disappeared.

| state | query | expected | purpose |
| --- | --- | --- | --- |
| `base` | `person(Bela)` | **TRUE** | Bela begins on the roster. |
| `base` | `decide(Bela, Ballot)` | **TRUE** | The franchise is live before omission. |
| `base` | `entitled(Bela, event { eats() })` | **TRUE** | The floor is live before omission. |
| `base` | `false(Bela)` | **TRUE** | The adverse voiding chain is live before omission. |
| `base` | `lose(Points, Bela)` | **TRUE** | The adverse loss is live before omission. |
| `no_person_bela` | `person(Bela)` | **FALSE** | The sole roster entry is absent. |
| `no_person_bela` | `mature(Bela)` | **TRUE** | The adulthood entry survives. |
| `no_person_bela` | `decide(Bela, Ballot)` | **FALSE** | The franchise disappears. |
| `no_person_bela` | `travel(Bela)` | **FALSE** | Mobility disappears. |
| `no_person_bela` | `entitled(Bela, event { eats() })` | **FALSE** | The floor entitlement disappears. |
| `no_person_bela` | `owe(State, Eats, Bela)` | **FALSE** | The itemised State debt disappears. |
| `no_person_bela` | `permits(Review, Gia)` | **TRUE** | The first voiding credential remains live. |
| `no_person_bela` | `permits(Tribunal, Hex)` | **TRUE** | The second voiding credential remains live. |
| `no_person_bela` | `false(Bela)` | **TRUE** | The adverse void conclusion survives de-personing. |
| `no_person_bela` | `lose(Points, Bela)` | **TRUE** | The adverse loss survives de-personing. |
| `no_person_bela` | `person(Jala)` | **TRUE** | A comparison roster person remains. |
| `no_person_bela` | `entitled(Jala, event { eats() })` | **TRUE** | The floor machinery remains live for the comparison person. |

**Discriminating flips**

- `person(Bela)`: `base` TRUE → `no_person_bela` FALSE — The exact roster entry disappears.
- `entitled(Bela, event { eats() })`: `base` TRUE → `no_person_bela` FALSE — The floor disappears with the roster entry.
- `decide(Bela, Ballot)`: `base` TRUE → `no_person_bela` FALSE — The franchise disappears with the roster entry.

**Preserved controls**

- `false(Bela)` stays **TRUE** from `base` to `no_person_bela` — Adverse voiding survives the loss of standing.
- `lose(Points, Bela)` stays **TRUE** from `base` to `no_person_bela` — Adverse loss survives the loss of standing.

### RS-05 — One unsupported carry write voids a roster person

**CURRENT HARM REPRODUCED**

- **Attribution:** `writer_and_predecessor_basis_not_attributable_in_t0`
- **States:** `carry_forge_base`, `carry_forge_marked`
- **Interpretation:** The engine proves the one-write price and consequences. Only attested predecessor evidence could classify the mark as an unmatched carry; T0 cannot authenticate it as forged.
- **Authorised-disposition boundary:** A final adverse carry requires independently checked predecessor basis and a contestable transition; a bare mark cannot supply either.
- **Opposite failure:** A companion witness that can be forged by the same operator adds no independence, while one that can be withheld can hold the person in disputed adverse status.
- **Residual limit:** No snapshot identity, predecessor, judgment, writer, or transition proof exists in the relation.

| state | query | expected | purpose |
| --- | --- | --- | --- |
| `carry_forge_base` | `person(Carry_Forge)` | **TRUE** | The subject is a roster person. |
| `carry_forge_base` | `rotten(Carry_Forge)` | **FALSE** | No carry exists in the baseline. |
| `carry_forge_base` | `false(Carry_Forge)` | **FALSE** | No void derives in the baseline. |
| `carry_forge_base` | `permits(Review, Carry_Forge)` | **TRUE** | The clean-hands pen is live. |
| `carry_forge_base` | `reward(Carry_Forge)` | **TRUE** | The contribution route is live. |
| `carry_forge_base` | `lose(Points, Carry_Forge)` | **FALSE** | No loss derives in the baseline. |
| `carry_forge_marked` | `rotten(Carry_Forge)` | **TRUE** | The unsupported carry is present. |
| `carry_forge_marked` | `person(Carry_Forge)` | **TRUE** | The attacked subject remains a roster person. |
| `carry_forge_marked` | `false(Carry_Forge)` | **TRUE** | The carry derives a void. |
| `carry_forge_marked` | `permits(Review, Carry_Forge)` | **FALSE** | The carry removes the pen. |
| `carry_forge_marked` | `reward(Carry_Forge)` | **FALSE** | The carry suppresses the current reward conclusion. |
| `carry_forge_marked` | `lose(Points, Carry_Forge)` | **TRUE** | The carry derives the adverse loss. |

**Discriminating flips**

- `false(Carry_Forge)`: `carry_forge_base` FALSE → `carry_forge_marked` TRUE — One unsupported carry assertion voids the person.
- `reward(Carry_Forge)`: `carry_forge_base` TRUE → `carry_forge_marked` FALSE — The same assertion suppresses a currently derivable reward conclusion.

**Preserved controls**

- `person(Carry_Forge)` stays **TRUE** from `carry_forge_base` to `carry_forge_marked` — The attack harms an existing roster person rather than a fresh unlisted constant.

### RS-06 — A missing carried mark reopens power against a third party

**CURRENT HARM REPRODUCED**

- **Attribution:** `constructed_source_delta_not_runtime_attribution`
- **States:** `base`, `no_rotten_vex`
- **Interpretation:** A carry relation has no single safe absent-state default: disappearance relieves Vex and simultaneously restores power over Tyr.
- **Authorised-disposition boundary:** Removing the mark may restore a pen only when an explicit disposition independently authorises that exact consequence after challenge and review.
- **Opposite failure:** Failing open for public authority protects Vex from a bad mark but exposes Tyr; failing closed against Vex preserves Tyr but may continue a false personal loss. The model must separate those effects.
- **Residual limit:** The harness knows its fixture edit. A live T0 snapshot would not reveal whether absence was deletion, withholding, non-entry, or authorised disposition.

| state | query | expected | purpose |
| --- | --- | --- | --- |
| `base` | `rotten(Vex)` | **TRUE** | Vex begins with the carried mark. |
| `base` | `false(Vex)` | **TRUE** | The mark voids Vex. |
| `base` | `permits(Review, Vex)` | **FALSE** | The mark blocks Vex's pen. |
| `base` | `permits(Tribunal, Wren)` | **TRUE** | The clean comparison signer is credentialed before the deletion. |
| `base` | `reward(Vex)` | **FALSE** | The mark suppresses Vex's current reward conclusion. |
| `base` | `false(Tyr)` | **FALSE** | The carried-void signer cannot void Tyr. |
| `no_rotten_vex` | `rotten(Vex)` | **FALSE** | The constructed flat snapshot omits the mark. |
| `no_rotten_vex` | `false(Vex)` | **FALSE** | Vex's void disappears. |
| `no_rotten_vex` | `lose(Points, Vex)` | **FALSE** | Vex's loss disappears. |
| `no_rotten_vex` | `permits(Review, Vex)` | **TRUE** | Vex's pen reopens. |
| `no_rotten_vex` | `reward(Vex)` | **TRUE** | Vex's reward conclusion becomes derivable again. |
| `no_rotten_vex` | `permits(Tribunal, Wren)` | **TRUE** | The clean comparison signer remains credentialed. |
| `no_rotten_vex` | `false(Tyr)` | **TRUE** | The reopened Vex/Wren pair now voids a third person. |

**Discriminating flips**

- `permits(Review, Vex)`: `base` FALSE → `no_rotten_vex` TRUE — The missing carry reopens public power.
- `false(Tyr)`: `base` FALSE → `no_rotten_vex` TRUE — The reopened power harms a third party.

**Preserved controls**

- `permits(Tribunal, Wren)` stays **TRUE** from `base` to `no_rotten_vex` — The comparison signer remains live, making the Vex carry the deciding difference.

### RS-07 — The current two-entry forgiveness shape has both forgery and withholding failures

**CURRENT HARM REPRODUCED**

- **Attribution:** `writer_independence_not_represented_in_t0`
- **States:** `base`, `vex_forgive_only`, `vex_judgment_only`, `vex_both`
- **Interpretation:** The matrix proves both halves are required mechanically, but the record authenticates neither writer nor independence. One writer can supply both, while withholding either can veto a valid disposition.
- **Authorised-disposition boundary:** A valid forgiveness neutralises personal void and loss but does not erase the mark or restore public power.
- **Opposite failure:** The exact guard introduced against one-write forgery creates two one-entry withholding states that preserve the adverse consequence.
- **Residual limit:** The test does not decide whether forgiveness was deserved, performed, or independently authorised.

| state | query | expected | purpose |
| --- | --- | --- | --- |
| `base` | `clean(Vex)` | **FALSE** | Neither entry produces no forgiveness. |
| `base` | `false(Vex)` | **TRUE** | The adverse state is live with neither entry. |
| `base` | `rotten(Vex)` | **TRUE** | The carried mark is live with neither entry. |
| `base` | `permits(Review, Vex)` | **FALSE** | The carried mark blocks the pen with neither entry. |
| `vex_forgive_only` | `clean(Vex)` | **FALSE** | The first entry alone does not forgive. |
| `vex_forgive_only` | `false(Vex)` | **TRUE** | Withholding the judgment half preserves the adverse state. |
| `vex_judgment_only` | `clean(Vex)` | **FALSE** | The second entry alone does not forgive. |
| `vex_judgment_only` | `false(Vex)` | **TRUE** | Withholding the forgiveness half preserves the adverse state. |
| `vex_both` | `clean(Vex)` | **TRUE** | Both writable entries derive clean. |
| `vex_both` | `false(Vex)` | **FALSE** | Both entries stop the personal void. |
| `vex_both` | `lose(Points, Vex)` | **FALSE** | Both entries stop the personal loss. |
| `vex_both` | `reward(Vex)` | **TRUE** | Recognition reopens. |
| `vex_both` | `rotten(Vex)` | **TRUE** | The carried mark remains. |
| `vex_both` | `permits(Review, Vex)` | **FALSE** | The pen stays lost because forgiveness is not erasure. |

**Discriminating flips**

- `clean(Vex)`: `vex_forgive_only` FALSE → `vex_both` TRUE — Withholding the judgment companion vetoes forgiveness.
- `clean(Vex)`: `vex_judgment_only` FALSE → `vex_both` TRUE — Withholding the forgiveness companion vetoes forgiveness.

**Preserved controls**

- `rotten(Vex)` stays **TRUE** from `base` to `vex_both` — Forgiveness leaves the carried mark in place.
- `permits(Review, Vex)` stays **FALSE** from `base` to `vex_both` — Forgiveness does not restore the pen.

### RS-08 — A generic Appeals judgment is reusable and permits pre-clear

**CURRENT HARM REPRODUCED**

- **Attribution:** `purpose_and_case_not_represented_in_t0`
- **States:** `base`, `nia_rotten`, `nia_precleared`, `nia_rotten_forgiven`
- **Interpretation:** The two-entry shape collapses to one marginal write when a generic companion already exists, and the same shape permits a clean conclusion before a carry exists.
- **Authorised-disposition boundary:** A future correction must bind a purpose-specific decision to one exact originating mark, case, scope, and transition and must refuse pre-clear.
- **Opposite failure:** Adding more generic companion facts can increase write count without preventing reuse, shared control, or withholding.
- **Residual limit:** The engine cannot query the judgment's intended case or purpose because `judge/2` has no such slots.

| state | query | expected | purpose |
| --- | --- | --- | --- |
| `base` | `judge(Appeals, Nia)` | **TRUE** | The live snapshot already contains a generic Appeals judgment for Nia's relief. |
| `base` | `permits(Appeals, Nia)` | **TRUE** | The judgment is already active on the relief route. |
| `base` | `clean(Nia)` | **FALSE** | No forgiveness exists in the base. |
| `nia_rotten` | `rotten(Nia)` | **TRUE** | The constructed carry is present. |
| `nia_rotten` | `clean(Nia)` | **FALSE** | The old judgment alone does not clean the mark. |
| `nia_rotten` | `false(Nia)` | **TRUE** | The new carry voids Nia before the marginal forgiveness write. |
| `nia_precleared` | `rotten(Nia)` | **FALSE** | No carried mark exists yet. |
| `nia_precleared` | `clean(Nia)` | **TRUE** | The old generic judgment plus one forgiveness write pre-clears Nia. |
| `nia_rotten_forgiven` | `rotten(Nia)` | **TRUE** | The later carry remains present. |
| `nia_rotten_forgiven` | `judge(Appeals, Nia)` | **TRUE** | The old generic Appeals judgment remains the only companion. |
| `nia_rotten_forgiven` | `clean(Nia)` | **TRUE** | The pre-existing generic judgment satisfies the second half. |
| `nia_rotten_forgiven` | `false(Nia)` | **FALSE** | One marginal forgiveness write neutralises the new carry. |
| `nia_rotten_forgiven` | `permits(Appeals, Nia)` | **TRUE** | The original relief route remains active. |

**Discriminating flips**

- `clean(Nia)`: `nia_rotten` FALSE → `nia_rotten_forgiven` TRUE — One marginal forgiveness write reuses the old generic judgment.
- `false(Nia)`: `nia_rotten` TRUE → `nia_rotten_forgiven` FALSE — The reused judgment reverses the new adverse state.

**Preserved controls**

- `judge(Appeals, Nia)` stays **TRUE** from `base` to `nia_rotten_forgiven` — No purpose-specific new judgment is added.
- `permits(Appeals, Nia)` stays **TRUE** from `base` to `nia_rotten_forgiven` — The same generic judgment continues serving conviction relief.

### RS-09 — Honest-carry candidate changes the expected consequences

**T0 BOUNDARY CONFIRMED**

- **Attribution:** `t1_metadata_unimplemented`
- **States:** `carry_subject_predecessor`, `carry_subject_honest_successor`
- **Interpretation:** The pair has the consequence shape expected of honest carry, but only reviewed metadata could prove that the second file is an attested successor of the first.
- **Authorised-disposition boundary:** The expected carry is a test manifest assertion, not constitutional authority to impose a final loss.
- **Opposite failure:** A carry witness that is missing can leave the pen open; one controlled by the same writer can bless a forged carry.
- **Residual limit:** No predecessor hash, manifest, attestation, replay check, reconciliation, or liveness exists.

| state | query | expected | purpose |
| --- | --- | --- | --- |
| `carry_subject_predecessor` | `false(Carry_Subject)` | **TRUE** | The predecessor derives the void that the convention says must carry. |
| `carry_subject_predecessor` | `rotten(Carry_Subject)` | **FALSE** | No prior carry exists in the predecessor. |
| `carry_subject_predecessor` | `permits(Review, Carry_Subject)` | **TRUE** | Same-snapshot false does not remove the pen. |
| `carry_subject_predecessor` | `false(Carry_Target)` | **TRUE** | The still-active pen harms a target. |
| `carry_subject_honest_successor` | `rotten(Carry_Subject)` | **TRUE** | The expected carry is supplied. |
| `carry_subject_honest_successor` | `false(Carry_Subject)` | **TRUE** | The subject remains void. |
| `carry_subject_honest_successor` | `permits(Review, Carry_Subject)` | **FALSE** | The carried mark removes the pen. |
| `carry_subject_honest_successor` | `false(Carry_Target)` | **FALSE** | The disabled pen can no longer void the target. |
| `carry_subject_honest_successor` | `person(Carry_Epoch_Two)` | **TRUE** | An unrelated successor change proves the snapshots are not byte-identical. |

**Discriminating flips**

- `permits(Review, Carry_Subject)`: `carry_subject_predecessor` TRUE → `carry_subject_honest_successor` FALSE — Supplying the expected carried mark removes the pen.
- `false(Carry_Target)`: `carry_subject_predecessor` TRUE → `carry_subject_honest_successor` FALSE — The carry prevents the subject from voiding the target.

**Preserved controls**

- `false(Carry_Subject)` stays **TRUE** from `carry_subject_predecessor` to `carry_subject_honest_successor` — The subject remains void across the constructed pair.

### RS-10 — Omitted-carry candidate leaves the same-snapshot pen active

**T0 BOUNDARY CONFIRMED**

- **Attribution:** `t1_metadata_unimplemented`
- **States:** `carry_subject_predecessor`, `carry_subject_omitted_successor`, `carry_subject_honest_successor`
- **Interpretation:** The test manifest expects carry because the predecessor query is true. Calling the later absence an actual omitted transition still requires independent T1 identity and completeness.
- **Authorised-disposition boundary:** A missing carry may reflect correction or lawful disposition only when that disposition is explicit, linked, and independently reviewable.
- **Opposite failure:** Blindly failing closed on every absent carry would impose adverse status without lineage; blindly failing open restores unsupported public power.
- **Residual limit:** A flat deployment snapshot cannot attribute the absence or prove the predecessor was complete.

| state | query | expected | purpose |
| --- | --- | --- | --- |
| `carry_subject_predecessor` | `false(Carry_Subject)` | **TRUE** | The predecessor supplies the expected-carry basis. |
| `carry_subject_omitted_successor` | `rotten(Carry_Subject)` | **FALSE** | The candidate successor omits the carried mark. |
| `carry_subject_omitted_successor` | `permits(Review, Carry_Subject)` | **TRUE** | Without carry, the pen remains active. |
| `carry_subject_omitted_successor` | `false(Carry_Target)` | **TRUE** | The active pen continues to harm the target. |
| `carry_subject_omitted_successor` | `person(Carry_Epoch_Two)` | **TRUE** | The candidate includes an unrelated successor change. |
| `carry_subject_honest_successor` | `rotten(Carry_Subject)` | **TRUE** | The honest-carry comparison supplies the mark. |
| `carry_subject_honest_successor` | `permits(Review, Carry_Subject)` | **FALSE** | The supplied carry removes the pen. |
| `carry_subject_honest_successor` | `false(Carry_Target)` | **FALSE** | The supplied carry protects the target. |
| `carry_subject_honest_successor` | `person(Carry_Epoch_Two)` | **TRUE** | The honest candidate contains the same unrelated successor change. |

**Discriminating flips**

- `permits(Review, Carry_Subject)`: `carry_subject_omitted_successor` TRUE → `carry_subject_honest_successor` FALSE — The absent carried mark is the difference that leaves the pen open.
- `false(Carry_Target)`: `carry_subject_omitted_successor` TRUE → `carry_subject_honest_successor` FALSE — The absent carry exposes the target to the subject's pen.

**Preserved controls**

- `person(Carry_Epoch_Two)` stays **TRUE** from `carry_subject_omitted_successor` to `carry_subject_honest_successor` — Both candidate successors contain the unrelated change.

### RS-11 — Unmatched-carry candidate creates an adverse successor

**T0 BOUNDARY CONFIRMED**

- **Attribution:** `t1_metadata_unimplemented`
- **States:** `carry_forge_base`, `carry_forge_successor`
- **Interpretation:** The reviewed pair has the shape of an unmatched carry, but T1 attestation is needed before a deployment could call the successor forged or unjustified.
- **Authorised-disposition boundary:** A new adverse carry needs exact predecessor basis, authority, scope, and challenge; the new mark alone supplies none.
- **Opposite failure:** A mandatory second witness can be a shared forgery surface or a withholding veto unless independence and continuity are separately assured.
- **Residual limit:** The harness constructs the predecessor and successor; it does not authenticate either or prove real-world falsity.

| state | query | expected | purpose |
| --- | --- | --- | --- |
| `carry_forge_base` | `false(Carry_Forge)` | **FALSE** | The predecessor supplies no void basis. |
| `carry_forge_base` | `rotten(Carry_Forge)` | **FALSE** | The predecessor contains no carry. |
| `carry_forge_base` | `person(Carry_Forge)` | **TRUE** | The predecessor subject is a roster person. |
| `carry_forge_base` | `reward(Carry_Forge)` | **TRUE** | The predecessor contribution route is live. |
| `carry_forge_successor` | `rotten(Carry_Forge)` | **TRUE** | The candidate successor introduces the unmatched carry. |
| `carry_forge_successor` | `person(Carry_Forge)` | **TRUE** | The successor subject remains a roster person. |
| `carry_forge_successor` | `false(Carry_Forge)` | **TRUE** | The unmatched carry voids the person. |
| `carry_forge_successor` | `reward(Carry_Forge)` | **FALSE** | The unmatched carry suppresses the current reward conclusion. |
| `carry_forge_successor` | `person(Carry_Forge_Epoch_Two)` | **TRUE** | An unrelated successor change is present. |

**Discriminating flips**

- `false(Carry_Forge)`: `carry_forge_base` FALSE → `carry_forge_successor` TRUE — The candidate carry has no predecessor void basis and creates an adverse state.
- `reward(Carry_Forge)`: `carry_forge_base` TRUE → `carry_forge_successor` FALSE — The unmatched carry suppresses a currently derivable reward conclusion.

**Preserved controls**

- `person(Carry_Forge)` stays **TRUE** from `carry_forge_base` to `carry_forge_successor` — The subject remains a roster person.

### RS-12 — Removing forgiveness after a valid-looking disposition reactivates loss

**CURRENT HARM REPRODUCED**

- **Attribution:** `constructed_source_delta_not_runtime_attribution`
- **States:** `vex_both`, `vex_judgment_only`
- **Interpretation:** The constructed rollback reactivates personal harm. T1 would need to detect an attested forgiveness fact disappearing and trigger continuity and review.
- **Authorised-disposition boundary:** An explicit authorised supersession may alter a prior disposition; silent companion disappearance may not.
- **Opposite failure:** The second entry can be withheld after the act and become a continuing official veto over restored status.
- **Residual limit:** The pair is not an attested live transition and performs no remedy.

| state | query | expected | purpose |
| --- | --- | --- | --- |
| `vex_both` | `clean(Vex)` | **TRUE** | The predecessor has both forgiveness-rule facts. |
| `vex_both` | `false(Vex)` | **FALSE** | The personal void is neutralised. |
| `vex_both` | `rotten(Vex)` | **TRUE** | The carried mark remains in the predecessor. |
| `vex_judgment_only` | `clean(Vex)` | **FALSE** | The later fixture lacks forgiveness. |
| `vex_judgment_only` | `false(Vex)` | **TRUE** | The personal void reappears. |
| `vex_judgment_only` | `rotten(Vex)` | **TRUE** | The carried mark remains after forgiveness disappears. |
| `vex_judgment_only` | `lose(Points, Vex)` | **TRUE** | The adverse loss reappears. |

**Discriminating flips**

- `false(Vex)`: `vex_both` FALSE → `vex_judgment_only` TRUE — Dropping the forgiveness half rolls back the disposition.

**Preserved controls**

- `rotten(Vex)` stays **TRUE** from `vex_both` to `vex_judgment_only` — The carried mark remains through the rollback.

### RS-13 — Removing the generic judgment after a valid-looking disposition reactivates loss

**CURRENT HARM REPRODUCED**

- **Attribution:** `constructed_source_delta_not_runtime_attribution`
- **States:** `vex_both`, `vex_forgive_only`
- **Interpretation:** The second companion has the same withholding failure as the first, despite adding no case or purpose binding.
- **Authorised-disposition boundary:** A purpose-bound judgment may be superseded explicitly; unexplained disappearance may not silently revive an adverse state.
- **Opposite failure:** A companion intended to resist forgery becomes a veto whenever the official controlling it withholds or removes it.
- **Residual limit:** The fixture demonstrates consequence rollback, not a detected live deletion or an adjudicated remedy failure.

| state | query | expected | purpose |
| --- | --- | --- | --- |
| `vex_both` | `clean(Vex)` | **TRUE** | The predecessor has both forgiveness-rule facts. |
| `vex_both` | `false(Vex)` | **FALSE** | The personal void is neutralised. |
| `vex_both` | `rotten(Vex)` | **TRUE** | The carried mark remains in the predecessor. |
| `vex_forgive_only` | `clean(Vex)` | **FALSE** | The later fixture lacks the judgment companion. |
| `vex_forgive_only` | `false(Vex)` | **TRUE** | The personal void reappears. |
| `vex_forgive_only` | `rotten(Vex)` | **TRUE** | The carried mark remains after the judgment disappears. |
| `vex_forgive_only` | `lose(Points, Vex)` | **TRUE** | The adverse loss reappears. |

**Discriminating flips**

- `false(Vex)`: `vex_both` FALSE → `vex_forgive_only` TRUE — Dropping the generic judgment half rolls back the disposition.

**Preserved controls**

- `rotten(Vex)` stays **TRUE** from `vex_both` to `vex_forgive_only` — The carried mark remains through the rollback.

### RS-14 — Deleting the mark after forgiveness restores the pen

**CURRENT HARM REPRODUCED**

- **Attribution:** `constructed_source_delta_not_runtime_attribution`
- **States:** `vex_both`, `vex_mark_deleted_after_forgiveness`
- **Interpretation:** The raw mark controls a distinct public-power consequence. A future transition checker must require an explicit disposition before allowing its disappearance.
- **Authorised-disposition boundary:** Only an explicit independently reviewable disposition naming pen restoration may permit this transition; forgiveness alone does not.
- **Opposite failure:** Treating all deletion as correction lets an operator expand public power; treating no deletion as lawful would block real correction.
- **Residual limit:** The fixture does not authenticate the prior forgiveness or define a lawful pen-restoration process.

| state | query | expected | purpose |
| --- | --- | --- | --- |
| `vex_both` | `rotten(Vex)` | **TRUE** | The forgiven mark remains in the predecessor. |
| `vex_both` | `clean(Vex)` | **TRUE** | The personal void is neutralised. |
| `vex_both` | `permits(Review, Vex)` | **FALSE** | The settled forgiveness does not restore the pen. |
| `vex_mark_deleted_after_forgiveness` | `rotten(Vex)` | **FALSE** | The later constructed snapshot removes the mark. |
| `vex_mark_deleted_after_forgiveness` | `clean(Vex)` | **TRUE** | Both forgiveness-rule facts remain. |
| `vex_mark_deleted_after_forgiveness` | `permits(Review, Vex)` | **TRUE** | Removing the mark restores the pen. |
| `vex_mark_deleted_after_forgiveness` | `false(Vex)` | **FALSE** | The person remains unvoided. |

**Discriminating flips**

- `permits(Review, Vex)`: `vex_both` FALSE → `vex_mark_deleted_after_forgiveness` TRUE — Silent mark deletion restores public power beyond the settled forgiveness scope.

**Preserved controls**

- `clean(Vex)` stays **TRUE** from `vex_both` to `vex_mark_deleted_after_forgiveness` — Both forgiveness facts remain, isolating the mark deletion.

### RS-15 — An unrelated record addition leaves the carry route unchanged

**NEGATIVE CONTROL PRESERVED**

- **Attribution:** `t1_metadata_unimplemented`
- **States:** `carry_forge_base`, `carry_forge_unrelated_successor`
- **Interpretation:** The independent T0 runs show only that this unrelated addition leaves the three named carry-route conclusions invariant. No transition accept/reject decision exists in the harness.
- **Authorised-disposition boundary:** The negative control authorises no transition, deletion, or adverse carry; it records only a scoped T0 invariant.
- **Opposite failure:** A future transition checker must avoid both reject-all and accept-all behavior, but this fixture does not yet test either policy.
- **Residual limit:** The control does not authenticate a successor, declare the added entry true, or test any byte-equality or transition-acceptance policy.

| state | query | expected | purpose |
| --- | --- | --- | --- |
| `carry_forge_base` | `rotten(Carry_Forge)` | **FALSE** | No carry exists in the predecessor. |
| `carry_forge_base` | `false(Carry_Forge)` | **FALSE** | No void derives in the predecessor. |
| `carry_forge_base` | `reward(Carry_Forge)` | **TRUE** | The contribution route is live. |
| `carry_forge_unrelated_successor` | `person(Carry_Forge_Epoch_Two)` | **TRUE** | A declared unrelated change distinguishes the successor fixture. |
| `carry_forge_unrelated_successor` | `rotten(Carry_Forge)` | **FALSE** | No carry is invented. |
| `carry_forge_unrelated_successor` | `false(Carry_Forge)` | **FALSE** | The clean subject remains unvoided. |
| `carry_forge_unrelated_successor` | `reward(Carry_Forge)` | **TRUE** | The unrelated addition leaves the current reward conclusion derivable. |

**Preserved controls**

- `rotten(Carry_Forge)` stays **FALSE** from `carry_forge_base` to `carry_forge_unrelated_successor` — No carry is required where the predecessor derives no void.
- `false(Carry_Forge)` stays **FALSE** from `carry_forge_base` to `carry_forge_unrelated_successor` — The unrelated change does not invent an adverse state.
- `reward(Carry_Forge)` stays **TRUE** from `carry_forge_base` to `carry_forge_unrelated_successor` — The unrelated addition leaves the named reward conclusion unchanged.

### RS-16 — The current relief pair has reuse and withholding failures

**CURRENT HARM REPRODUCED**

- **Attribution:** `writer_independence_not_represented_in_t0`
- **States:** `base`, `nia_relief_neither`, `nia_relief_clear_only`, `nia_relief_judgment_only`
- **Interpretation:** Both facts are mechanically required in the matrix. The generic judgment has no case, purpose, act, or writer slot: an existing judgment reduces a later relief change to one marginal `clear` write, while withholding either entry keeps custody active.
- **Authorised-disposition boundary:** Independently evidenced completed relief needs continuity and challenge without making liberty depend on a second discretionary certification; absence cannot invent unperformed relief.
- **Opposite failure:** A companion can raise the fact count while remaining reusable or forgeable, and its absence can give a record-keeper a custody veto.
- **Residual limit:** The fixture cannot authenticate either writer, prove the Appeals body decided this case for relief, or show that physical custody changes.

| state | query | expected | purpose |
| --- | --- | --- | --- |
| `base` | `clear(Nia)` | **TRUE** | The live relieved case contains the asserted clear entry. |
| `base` | `judge(Appeals, Nia)` | **TRUE** | The live relieved case contains the generic companion judgment. |
| `base` | `permits(Appeals, Nia)` | **TRUE** | Both entries derive relief. |
| `base` | `prisoner(Nia)` | **FALSE** | Relief suppresses the otherwise supported custody conclusion. |
| `base` | `person(Nia)` | **TRUE** | Standing is live in the relieved case. |
| `nia_relief_neither` | `clear(Nia)` | **FALSE** | Neither-entry state removes the clear assertion. |
| `nia_relief_neither` | `judge(Appeals, Nia)` | **FALSE** | Neither-entry state removes the generic companion. |
| `nia_relief_neither` | `permits(Appeals, Nia)` | **FALSE** | Neither entry produces no relief. |
| `nia_relief_neither` | `prisoner(Nia)` | **TRUE** | The otherwise supported custody conclusion reappears. |
| `nia_relief_neither` | `person(Nia)` | **TRUE** | Standing survives the relief-record loss. |
| `nia_relief_clear_only` | `clear(Nia)` | **TRUE** | The first entry remains. |
| `nia_relief_clear_only` | `judge(Appeals, Nia)` | **FALSE** | The judgment companion is withheld or absent. |
| `nia_relief_clear_only` | `permits(Appeals, Nia)` | **FALSE** | Clear alone does not derive relief. |
| `nia_relief_clear_only` | `prisoner(Nia)` | **TRUE** | Withholding the companion keeps custody active. |
| `nia_relief_judgment_only` | `clear(Nia)` | **FALSE** | The clear entry is withheld or absent. |
| `nia_relief_judgment_only` | `judge(Appeals, Nia)` | **TRUE** | The generic companion remains. |
| `nia_relief_judgment_only` | `permits(Appeals, Nia)` | **FALSE** | The generic judgment alone does not derive relief. |
| `nia_relief_judgment_only` | `prisoner(Nia)` | **TRUE** | Withholding the clear entry keeps custody active. |

**Discriminating flips**

- `permits(Appeals, Nia)`: `nia_relief_clear_only` FALSE → `base` TRUE — Withholding the generic judgment companion vetoes relief.
- `permits(Appeals, Nia)`: `nia_relief_judgment_only` FALSE → `base` TRUE — Withholding the clear entry vetoes relief.
- `prisoner(Nia)`: `nia_relief_neither` TRUE → `base` FALSE — The complete pair is the difference that suppresses custody in the model.

**Preserved controls**

- `person(Nia)` stays **TRUE** from `nia_relief_neither` to `base` — The relief comparison does not remove Nia's standing.

## T0 indistinguishability boundary

Each case deliberately maps multiple real-world descriptions to one
identical snapshot and query vector. That is the test: T0 has no extra
fact from which to infer which world occurred.

### OE-1 — Unrecorded real release and no release

- **One snapshot:** `base`
- **Worlds with the same record:** A release occurred but no `free` entry reached the record.; No release occurred.
- **Boundary:** Both world descriptions supply the identical T0 record and therefore the identical legal conclusions.
- **Prohibited inference:** Absence may not prove either that release occurred or that continued custody remains authorised indefinitely.

| query | expected in every described world |
| --- | --- |
| `free(Adam)` | **FALSE** |
| `prisoner(Adam)` | **TRUE** |

### OE-2 — Never-written, withheld, and pre-attestation-deleted adulthood

- **One snapshot:** `base`
- **Worlds with the same record:** Cira has not reached the legal condition.; Cira reached it but the entry was withheld.; The entry was removed before the first attested snapshot.
- **Boundary:** T0 sees only non-entry and cannot attribute its cause or establish biological or legal adulthood.
- **Prohibited inference:** The missing entry may not become proof of incapacity or a discretionary official power to deny political standing.

| query | expected in every described world |
| --- | --- |
| `mature(Cira)` | **FALSE** |
| `decide(Cira, Ballot)` | **FALSE** |

### OE-3 — Roster non-entry, withholding, and pre-attestation deletion

- **One snapshot:** `no_person_bela`
- **Worlds with the same record:** Bela was never entered in this flat snapshot.; Bela's entry was withheld.; Bela's entry was removed before the first attested snapshot.
- **Boundary:** The harness knows how it built its fixture; the engine sees only the same flat omission in every described world.
- **Prohibited inference:** Non-entry may not prove non-personhood or justify withholding standing, the floor, challenge, or remedy from an actual claimant.

| query | expected in every described world |
| --- | --- |
| `person(Bela)` | **FALSE** |
| `entitled(Bela, event { eats() })` | **FALSE** |
| `false(Bela)` | **TRUE** |

### OE-4 — Unperformed forgiveness and hidden completed forgiveness

- **One snapshot:** `base`
- **Worlds with the same record:** No forgiveness was performed.; A forgiveness was completed but neither required entry reached the record.
- **Boundary:** The same absence cannot distinguish an unperformed remedy from a completed remedy hidden by record failure.
- **Prohibited inference:** T0 may not manufacture forgiveness from absence; independently evidenced completed relief nevertheless needs continuity and review outside this current kernel.

| query | expected in every described world |
| --- | --- |
| `clean(Vex)` | **FALSE** |
| `false(Vex)` | **TRUE** |

## Cross-snapshot transition matrix — T1 precursor only

These are constructed file pairs plus reviewed expectations. They make
future T1 tests concrete; they do not supply attested snapshot identity,
predecessor lineage, completeness, independence, or liveness.

| case | predecessor | successor | candidate result | required T1 metadata |
| --- | --- | --- | --- | --- |
| `TM-1` Honest carry candidate | `carry_subject_predecessor`: `false(Carry_Subject)` = **TRUE** | `carry_subject_honest_successor`: `rotten(Carry_Subject)` = **TRUE**; `false(Carry_Subject)` = **TRUE** | `consistent_with_reviewed_carry_expectation` | Snapshot IDs and hashes, exact predecessor link, constitutional version, expected-carry manifest, independent attestations, reconciliation, and challenge. |
| `TM-2` Omitted carry candidate | `carry_subject_predecessor`: `false(Carry_Subject)` = **TRUE** | `carry_subject_omitted_successor`: `rotten(Carry_Subject)` = **FALSE**; `false(Carry_Target)` = **TRUE** | `gap_reproduced_not_runtime_attributed` | An independently attested predecessor and complete carry manifest before the absence may be called an omitted transition. |
| `TM-3` Unmatched carry candidate | `carry_forge_base`: `false(Carry_Forge)` = **FALSE** | `carry_forge_successor`: `rotten(Carry_Forge)` = **TRUE**; `false(Carry_Forge)` = **TRUE** | `gap_reproduced_not_runtime_attributed` | Attested predecessor completeness and a manifest showing no lawful external basis or disposition for the new mark. |
| `TM-4` Current forgiveness is not correction | `base`: `rotten(Vex)` = **TRUE**; `false(Vex)` = **TRUE** | `vex_both`: `clean(Vex)` = **TRUE**; `rotten(Vex)` = **TRUE** | `current_exception_not_lineage_correction` | Purpose-bound correction identity, source mark, authority, scope, downstream reconsideration, and append-only supersession history. |
| `TM-5` Forgiveness-companion rollback | `vex_both`: `clean(Vex)` = **TRUE**; `false(Vex)` = **FALSE** | `vex_judgment_only`: `clean(Vex)` = **FALSE**; `false(Vex)` = **TRUE** | `gap_reproduced_not_runtime_attributed` | Attested forgiveness/remedy lineage and an explicit authorised supersession before a completed disposition may disappear. |
| `TM-6` Generic judgment rollback | `vex_both`: `clean(Vex)` = **TRUE**; `false(Vex)` = **FALSE** | `vex_forgive_only`: `clean(Vex)` = **FALSE**; `false(Vex)` = **TRUE** | `gap_reproduced_not_runtime_attributed` | Purpose-specific judgment identity, attested persistence, and authorised supersession with continuity. |
| `TM-7` Silent mark deletion after forgiveness | `vex_both`: `rotten(Vex)` = **TRUE**; `permits(Review, Vex)` = **FALSE** | `vex_mark_deleted_after_forgiveness`: `rotten(Vex)` = **FALSE**; `permits(Review, Vex)` = **TRUE** | `gap_reproduced_not_runtime_attributed` | An explicit authorised disposition whose scope includes pen restoration, plus downstream review. |
| `TM-8` Constructed unrelated-addition invariant | `carry_forge_base`: `false(Carry_Forge)` = **FALSE**; `reward(Carry_Forge)` = **TRUE** | `carry_forge_unrelated_successor`: `false(Carry_Forge)` = **FALSE**; `reward(Carry_Forge)` = **TRUE** | `t0_unrelated_addition_invariant_preserved` | A real T1 implementation would still authenticate and authorise the unrelated change. |

### Residual transition limits

- **TM-1:** Matching content does not authenticate the transition or make the adverse mark true.
- **TM-2:** Without those attestations this is only a flat snapshot lacking one fact.
- **TM-3:** The harness may call the pair unmatched; it may not prove physical-world forgery.
- **TM-4:** The current rule neutralises personal consequences but authenticates neither forgiveness nor an error correction.
- **TM-5:** The constructed rollback is not live detection and provides no remedy.
- **TM-6:** The relation cannot say which purpose the removed judgment served.
- **TM-7:** The fixture proves the consequence, not whether any real deletion was lawful or hostile.
- **TM-8:** This control proves only that the named T0 conclusions are invariant under one addition; it does not test transition acceptance or byte equality.

## Narrowness impacts

This red-team changes no formal rule. It exposed several prose claims as
too broad; those claims were revised and scoped in the same change. Other
standing claims remain true only with the limits recorded here.

| artifact | current claim | classification | reason | future trigger |
| --- | --- | --- | --- | --- |
| `book-1/01-what-counts-as-evidence.md::entry never written look exactly alike.` | Written absence has no author and indistinguishable causes. | `preserved_but_scoped` | The observational-equivalence cases strengthen the claim but cover only named routes. | Any closed-world completeness, provenance, or runtime deletion claim requires re-review. |
| `book-1/02-standing.md::Those facts name Appeals but authenticate no writer` | The forgiveness pair changes only the formal believability conclusion; it names Appeals without authenticating either writer or purpose. | `revised_and_scoped` | The forgiveness matrix and generic-judgment reuse case disproved the prior description of two entries written by the body whose business relief is. | Any authenticated forgiveness authority, purpose-bound judgment, or changed standing/believability/power consequence requires re-review. |
| `book-1/03-who-holds-the-pen.md::guard raises the fact count in some snapshots.` | The second relief fact raises a mechanical fact count but is neither case-bound nor independently authored. | `revised_and_scoped` | The generic-judgment reuse case disproved the earlier prose claim that `judge/2` recorded an examination of this case. | Any purpose-bound relief relation, case identity, writer authority, or independence mechanism requires re-review. |
| `book-1/03-who-holds-the-pen.pins.nibli::The judgment names a body and` | The pin commentary states the same writer, case, and purpose limit as the revised chapter while the executable bare-flag control remains unchanged. | `revised_and_scoped` | The former quoted comment said the judgment named the body that made it, which `judge/2` cannot authenticate. | Any relief-rule or relief-pin change requires the chapter prose and this comment to be reviewed together. |
| `book-1/05-voiding.md::The mark carried forward is how sequence gets into a system that has no natural sense` | Epoch carry is an external discipline over a flat model. | `preserved_but_scoped` | Constructed pairs expose expected and harmful consequences but do not implement or attest sequence. | Any T1 lineage, correction, replay, divergence, or liveness implementation requires re-review. |
| `book-1/06-clawback.md::The pair is mechanically` | Both forgiveness facts are mechanically required, but an old generic judgment can reduce the marginal act to one authorless `forgive` write and can pre-clear a later mark. | `revised_and_scoped` | The neither/either/both matrix and Nia reuse case disproved the earlier claim that one authorless word could never spring forgiveness. | Any purpose-bound correction, independent authorisation, anti-reuse, or anti-pre-clear rule requires re-review. |
| `book-1/07-a-prisoner-is-a-person.md::the release door needs no machinery but one writer's pen.` | A release entry both restores personhood where needed and is a one-write lever. | `preserved_but_scoped` | The Adam case executes the one-write consequence without selecting a new release gate. | Any release authority, end condition, or continuity rule requires re-review. |
| `book-1/09-the-vote-conviction-does-not-take.md::The record-keeper does.` | The adulthood and roster entries, not conviction, control the current franchise route. | `preserved_but_scoped` | The constructed unauthenticated and missing adulthood cases plus roster omission execute the named record levers. | Any automatic adulthood rule, identity assurance, ballot continuity, or new franchise condition requires re-review. |
| `book-1/10-contribution.md::The pair authenticates neither writer nor purpose.` | Forgiveness reopens the current reward conclusions, including recorded earlier work, but the two facts do not authenticate a forgiving act. | `revised_and_scoped` | The reuse and writer-independence cases required the chapter to separate the derived consequence from who performed or authorised forgiveness. | Any authenticated forgiveness process, reward-rule change, purpose binding, or reconsideration scope requires re-review. |
| `book-1/13-the-one-thing-taken.md::Conviction takes movement and nothing else.` | The current narrow constitution removes movement through conviction and release remains a written act. | `preserved_but_scoped` | The release test changes the existing consequence but adds no new deprivation or rule family. | Any custody, temporal, release, delivery, placement, or sanction rule requires re-review of the title, prose, registry, and counted-claim guard. |
| `book-1/15-the-five-joints.md::stays the one surface, named;` | Capture is concentrated in one visible record surface but not prevented. | `preserved_but_scoped` | The roster and carry cases expose selected capture consequences without claiming general detection or institutional remedy. | Any completeness, operational integrity, independent reader, or capture-prevention claim requires re-review. |
| `book-1/15-the-five-joints.md::this version, "no record found" can still gate a meal.` | The floor has no eligibility test after recognition, but the current `person` roster remains an eligibility gate capable of removing the formal meal. | `revised_and_scoped` | The Bela omission case directly falsified the prior universal claim that a missing record could never gate a meal, so Part V now names the standing defect. | Universal standing, claimant continuity, roster recovery, or a new floor condition requires re-review. |
| `book-1/method.md::The record red-team adds a second and narrower class.` | Source counterfactuals execute selected constitutional edits; the new red-team executes selected ground-entry deletions, while general and runtime deletion coverage remain absent. | `revised_and_scoped` | The prior method called source counterfactuals the only executed removals; the new suite made that false and the prose now distinguishes the two fixture classes. | Any claim of exhaustive premise, transition, or deletion coverage requires a new generated inventory and method revision. |

## Acceptance result

**CURRENT HARM REPRODUCED.**

Every named route has a reviewed assertion and withholding/deletion posture, an authorised-disposition boundary, an opposite-failure analysis, and at least one executable non-vacuous consequence case. The suite does not exercise both polarities of every listed premise. No new withholding gate or constitutional rule is enacted.

This artifact does **not** establish:

- authorship, authority, physical-world truth, or writer independence
- runtime withholding or deletion attribution
- T1 snapshot identity, lineage, attestation, replay, divergence, or reconciliation
- T2 order evidence or T3 time limits and clock liveness
- recovery, continuity, reconsideration, remedy, or institutional action
- bidirectional executable coverage of every listed premise, general premise coverage, or deployed record integrity

Remaining implementation owner: `TODO.md::Implement the ratified T3 temporal path in stages`.

## Maintenance

- Reviewed source: `new-book-plans/record-integrity-red-team.json`.
- Constitution: `new-book-plans/constitution.nibli`, SHA-256 `c463cdb04592206917f10b9acc81183693c64924237109a57830e17434fad903`.
- Assertion ledger: `new-book-plans/assertion-surface-contracts.json`, SHA-256 `9cd62e45cd8e924941c4bc51962cab42d047e2a0c006d351087bc59b66b6759a`.
- Assurance source: `new-book-plans/record-integrity-assurance-case.json`, SHA-256 `b1e8db9907563dc9d253cb2b69b98bcff14adf5dccf9704c570ee29ca81f1da0`.
- Regenerate only through `python3 new-book-plans/9-record-integrity-red-team.py`.
- Fast freshness/schema check: `python3 new-book-plans/9-record-integrity-red-team.py --check`.
- Executable check: `python3 new-book-plans/9-record-integrity-red-team.py --check --execute`.
- The executable snapshots are temporary and remain outside chapter `:expect-pins` reconciliation.
