# ── Counterfactual: `choose(Electorate, Boss).` deleted ──────────────────────
# The same harm by the other route into standing, and this one jails the file's
# own honest whistleblower — the whole of chapter 2's argument, undone by one
# deleted fact.
:expect-pins 3

? authority(Boss).
# => FALSE

? defend(Rebel).
# => FALSE

? prisoner(Rebel).
# => TRUE
