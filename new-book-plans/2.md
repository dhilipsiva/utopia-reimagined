> **Superseded 2026-07-28 — dated correspondence, kept as history. Do not build on it.**
> The floor is now **eight** rights spelled `entitled(every person, event { P() })`,
> not six spelled `obligated`. This document's claim that the floor is protected by
> "being outside the machine entirely" is wrong: protection comes from the `event { }`
> abstraction putting P inside the `person` cone. See `TODO.md` and Article 1 of
> `utopia-v2.nibli`.

Spine computed rather than guessed — 34 predicates, 26 rules, 4 strata. Three things the computation turned up that change what the book is:

**I was partly wrong last turn.** I claimed the floor sits in the negation-free stratum. The individual rights *predicates* don't — `dwell`, `expresses` and `travel` all inherit non-monotonicity through `prisoner`. What is actually structurally unconditional is `obligated` itself: it appears in no rule head and no rule body, so no derivation can reach it. That's a better claim than the one I made, because it's exact. The floor isn't protected by sitting in a safe layer; it's protected by being outside the machine entirely.

**The machinery only exists where the system might be tempted to take something away.** `secure`, `eats`, `healthy` and `learn` each appear exactly once in the whole file — inside their own obligation, and nowhere else. Zero rules. Every rule in the constitution is about the accused, the convicted, the corrupt, or the accuser. That's the book's thesis and it fell out of the graph rather than being asserted: a right that needs a computation to establish is not a right, it's a benefit. Eligibility checks are where denial lives.

**Conviction deprives exactly one thing.** `travel` sits alone at the top of the graph behind `~prisoner`, and every other floor right survives conviction — where inheritance might have lost one, there's an explicit re-grant rule. Verified: `person(Hano)` TRUE, `expresses(Hano)` TRUE, `travel(Hano)` FALSE, `travel(Adam)` FALSE. The entire punishment apparatus, reduced to logic, takes away movement and nothing else. For a general reader that's a startling sentence, and it's a theorem.

The uncomfortable one: `eats(Adam)` FALSE, `healthy(Bela)` FALSE. Not denial — no rule connects any obligation to any fact about food or care reaching a person. The obligation layer is complete and the delivery layer doesn't exist. So Part I is writable now and a "does it actually arrive" chapter isn't, until the constitution grows a provisioning layer. I put that in the chapter-1 fidelity table as row 1.7 and I'd keep it visible in the prose — stating the gap is what separates this from propaganda, and a reader who catches you hiding it stops believing everything else.

Fourteen chapters in four parts, plus a fifth part that is explicitly *not* derived — the four strong chapters harvested from `book.md` are argument and evidence, and should look like a different kind of chapter so the reader can feel the seam rather than be fooled across it.

`strata.py` is included so the ordering is reproducible: point it at the constitution after any edit and the spine recomputes. If a chapter moves stratum, the book's structure has changed and you'll know before a reviewer does.

Next: chapter 1 is the one to draft first, and it's short — the floor, the six obligations, and the argument that computing eligibility is where denial lives. Say go and I'll write it in plain English at target length, with the fidelity table filled per paragraph.
